

import java.util.Scanner;
import java.util.Arrays;
import java.util.ArrayList;

public class AACH
{
	public static void main (String[] args)
	{
		Scanner input = new Scanner(System.in);
		
		int n = input.nextInt();
		
		input.nextLine();
		
		ArrayList<String> w = new ArrayList<String>();
		
		for (int i=0; i<n; i++)
		{
			String a = input.next();
			
			if ( w.contains(a) == false )
			{
				w.add(a);
			}
		}
		
		word[] wArr = new word[ w.size() ];
		
		for ( int i=0; i<w.size(); i++)
		{
			wArr[i] = new word( w.get(i) );
		}
		
		Arrays.sort(wArr);
		
		for (int i=0; i<wArr.length; i++)
		{
			System.out.println(wArr[i]);
		}
	}
	
	public static class word implements Comparable<word>
	{
		String s;
		
		word() { s = ""; }
		
		word(String s) { this.s = s; }
		
		@Override
		public int compareTo(word w)
		{
			if ( this.s.length() > w.s.length() )
				return 1;
			
			else if ( this.s.length() < w.s.length() )
				return -1;
			
			else
			{
				return this.s.compareTo(w.s);
			}
		}
		
		public String toString()
		{
			return s;
		}
	}
	
}
